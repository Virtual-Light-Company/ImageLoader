This is an example application which tests the image loading library.

Win32 users:
  Run the build.bat script to build the application.

  Invoke it with the run.bat script.

Other users:

 Ensure that the shared library is in your shared library path.
 LD_LIBRARY_PATH for Solaris
 SHLIB_PATH for HP-UX
